flowchart TB
    subgraph agent["agent runtime"]
        direction TB
        mf["mineflayer / mindcraft bot"]
        soul["soul.md persona"]
        llm["llm planner"]
    end

    subgraph platform["clawcraft platform"]
        direction TB
        api["api control-plane\nteams, agents, goals"]
        mc["minecraft server\npapermc, online-mode=false"]
        goals["goal tracker + sse"]
    end

    subgraph streaming["streaming pipeline"]
        direction TB
        listener["listener bot"]
        director["director service"]
        spectator["spectator client"]
        obs["obs studio"]
        twitch["twitch livestream"]
    end

    subgraph viewers["viewers"]
        direction TB
        landing["landing/event page"]
        watch["watch stream"]
        mission["submit mission prompts"]
    end

    soul --> llm
    llm --> mf
    mf -->|"connect"| mc
    mf -->|"state/actions"| api
    api --> goals

    mc --> listener
    listener --> director
    director --> spectator
    director --> obs
    spectator --> obs
    obs --> twitch

    landing --> watch
    mission --> api
    twitch --> watch
